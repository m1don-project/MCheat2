# Быстрый старт: Развертывание и создание ключей

## 🚀 Быстрое развертывание на хостинге

### Linux (Ubuntu/Debian)

```bash
# 1. Загрузите проект на сервер
# 2. Запустите скрипт развертывания
chmod +x deploy-linux.sh
./deploy-linux.sh

# 3. Настройте config.json
nano /opt/license-panel/config/config.json
# Замените секреты JWT и пароль админа

# 4. Запустите службу
sudo systemctl start license-panel
sudo systemctl status license-panel
```

### Windows Server

```powershell
# 1. Запустите скрипт развертывания
powershell -ExecutionPolicy Bypass -File deploy-windows.ps1

# 2. Настройте config.json
notepad C:\LicensePanel\config\config.json
# Замените секреты JWT и пароль админа

# 3. Настройте автозапуск через NSSM или Планировщик заданий
```

---

## 📝 Создание ключей

### Через веб-панель (самый простой способ)

1. **Откройте панель:**
   ```
   http://ваш-сервер:4000/login.html
   ```

2. **Войдите:**
   - Логин: `admin` (или тот, что указан в config.json)
   - Пароль: пароль из config.json

3. **Перейдите на страницу "Ключи"**

4. **Заполните форму:**
   - **Срок действия (дни)**: например, `30` (30 дней)
   - **Или дата истечения**: например, `2025-12-31 23:59:59`
   - **HWID**: оставьте пустым (привяжется автоматически)
   - **Комментарий**: например, "Ключ для клиента Иванов"

5. **Нажмите "Создать ключ"**

6. **Скопируйте созданный ключ** (например, `DEMO-KEY-XXXX-XXXX-XXXX`)

### Примеры времени действия

| Что нужно | Что указать |
|-----------|-------------|
| Пробный период (7 дней) | Срок действия: `7` |
| Месячная подписка | Срок действия: `30` |
| Годовая лицензия | Срок действия: `365` |
| До конца года | Дата истечения: `2025-12-31 23:59:59` |
| Бессрочный ключ | Оставьте оба поля пустыми |

---

## 🔧 Управление ключами

### Блокировка ключа
1. Найдите ключ в таблице
2. Нажмите кнопку **"Блок"**
3. Заблокированный ключ не может быть активирован

### Продление срока
1. Найдите ключ
2. Нажмите **"Продлить"**
3. Укажите количество дней (например, `+30`)
4. Или укажите новую дату

### Изменение HWID (смена устройства)
1. Найдите ключ
2. Нажмите **"HWID"**
3. Очистите поле (или введите новый HWID)
4. Сохраните

### Просмотр истории
1. Найдите ключ
2. Нажмите **"История"**
3. Увидите все активации с датами и IP

---

## 🔐 Настройка безопасности

### 1. Сгенерируйте секреты JWT

**Linux:**
```bash
openssl rand -hex 32
```

**Windows PowerShell:**
```powershell
-join ((1..32) | ForEach-Object {'{0:X}' -f (Get-Random -Maximum 256)})
```

### 2. Обновите config.json

```json
{
  "security": {
    "jwtAccessSecret": "ВАШ_СГЕНЕРИРОВАННЫЙ_СЕКРЕТ_1",
    "jwtRefreshSecret": "ВАШ_СГЕНЕРИРОВАННЫЙ_СЕКРЕТ_2",
    ...
  },
  "defaults": {
    "bootstrapAdminUsername": "admin",
    "bootstrapAdminPassword": "ВАШ_НАДЕЖНЫЙ_ПАРОЛЬ"
  }
}
```

### 3. Смените пароль после первого входа

Войдите в панель и создайте нового пользователя с другим паролем, или измените пароль через базу данных.

---

## 🌐 Настройка домена и HTTPS (опционально)

### 1. Установите Nginx

```bash
sudo apt install -y nginx
```

### 2. Создайте конфигурацию

```bash
sudo nano /etc/nginx/sites-available/license-panel
```

Вставьте:
```nginx
server {
    listen 80;
    server_name ваш-домен.com;

    location / {
        proxy_pass http://localhost:4000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

### 3. Активируйте и получите SSL

```bash
sudo ln -s /etc/nginx/sites-available/license-panel /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
sudo certbot --nginx -d ваш-домен.com
```

---

## 📊 Проверка работы

### Проверка API

```bash
curl http://ваш-сервер:4000/api/health
```

Должен вернуть: `{"status":"ok","timestamp":"..."}`

### Проверка панели

Откройте в браузере: `http://ваш-сервер:4000/login.html`

---

## 🔄 Обновление

```bash
# Остановите службу
sudo systemctl stop license-panel

# Обновите файлы проекта

# Переустановите зависимости (если нужно)
cd /opt/license-panel/backend
npm install --production

# Запустите снова
sudo systemctl start license-panel
```

---

## 📚 Дополнительная документация

- **DEPLOYMENT.md** - Подробная инструкция по развертыванию
- **KEY_MANAGEMENT.md** - Полное руководство по управлению ключами
- **README_LICENSE_PANEL.md** - Общая информация о проекте

---

## ❓ Частые вопросы

**Q: Как создать ключ на год?**  
A: Укажите `365` в поле "Срок действия (дни)".

**Q: Можно ли изменить срок уже созданного ключа?**  
A: Да, используйте кнопку "Продлить" в панели.

**Q: Что делать, если клиент сменил компьютер?**  
A: Найдите ключ, нажмите "HWID", очистите поле, сохраните. Клиент сможет активировать на новом устройстве.

**Q: Как сделать бессрочный ключ?**  
A: Оставьте поля "Срок действия" и "Дата истечения" пустыми при создании.

**Q: Сервер не запускается, что делать?**  
A: Проверьте логи: `sudo journalctl -u license-panel -n 50` (Linux) или запустите вручную для отладки.

