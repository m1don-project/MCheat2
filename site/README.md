# Панель управления лицензиями

Полнофункциональная веб-панель для управления лицензионными ключами программного обеспечения.

## 📁 Структура

```
site/
├── backend/          # Node.js сервер (REST API)
│   ├── server.js
│   ├── routes/       # API маршруты
│   ├── database/     # SQL схемы
│   └── utils/        # Утилиты (JWT, auth)
├── public/           # Frontend (HTML/CSS/JS)
│   ├── login.html
│   ├── dashboard.html
│   ├── users.html
│   ├── keys.html
│   ├── styles.css
│   └── script.js
├── config/           # Конфигурация
│   └── config.json
└── Документация...
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd site/backend
npm install
```

### 2. Настройка конфигурации

Отредактируйте `site/config/config.json`:

```json
{
  "server": {
    "host": "0.0.0.0",
    "port": 4000
  },
  "security": {
    "jwtAccessSecret": "ВАШ_СЕКРЕТ_1",
    "jwtRefreshSecret": "ВАШ_СЕКРЕТ_2",
    "accessTokenMinutes": 15,
    "refreshTokenDays": 7,
    "passwordSaltRounds": 10
  },
  "database": {
    "filename": "license.db"
  },
  "defaults": {
    "bootstrapAdminUsername": "admin",
    "bootstrapAdminPassword": "ВАШ_ПАРОЛЬ"
  }
}
```

**Важно:** Сгенерируйте секреты:
```bash
openssl rand -hex 32
```

### 3. Запуск сервера

```bash
cd site/backend
node server.js
```

Панель доступна на: `http://localhost:4000/login.html`

## 📚 Документация

- **[QUICK_START.md](QUICK_START.md)** - Быстрый старт и создание ключей
- **[DEPLOYMENT.md](DEPLOYMENT.md)** - Развертывание на хостинге (Linux/Windows)
- **[KEY_MANAGEMENT.md](KEY_MANAGEMENT.md)** - Управление ключами
- **[INTEGRATION_INFO.md](INTEGRATION_INFO.md)** - Интеграция с MCheat

## 🔧 Возможности

- ✅ Авторизация (JWT + Refresh Token)
- ✅ Управление пользователями
- ✅ Создание и управление лицензионными ключами
- ✅ Привязка к HWID
- ✅ Блокировка и продление ключей
- ✅ История активаций
- ✅ Клиентское API для активации/проверки

## 🌐 Развертывание

### Linux (systemd)
```bash
cd site
chmod +x deploy-linux.sh
./deploy-linux.sh
```

### Windows
```powershell
cd site
powershell -ExecutionPolicy Bypass -File deploy-windows.ps1
```

### Fly.io
См. инструкции в [DEPLOYMENT.md](DEPLOYMENT.md)

## 🔌 API Endpoints

### Административные (требуют JWT):
- `POST /api/auth/login` - Вход
- `GET /api/users` - Список пользователей
- `POST /api/users` - Создать пользователя
- `GET /api/keys` - Список ключей
- `POST /api/keys` - Создать ключ
- `PUT /api/keys/:id/block` - Блокировать ключ
- `PUT /api/keys/:id/extend` - Продлить ключ
- `PUT /api/keys/:id/hwid` - Изменить HWID

### Клиентские (для MCheat):
- `POST /api/activate_key` - Активация ключа
- `POST /api/check_key` - Проверка ключа
- `GET /api/get_remaining_days?key=...` - Остаток дней

## 🔐 Безопасность

- Пароли хешируются через bcrypt
- JWT токены с отдельными секретами
- Проверка ролей (admin/user)
- CORS защита
- Валидация входных данных

## 📝 Примеры использования

См. [KEY_MANAGEMENT.md](KEY_MANAGEMENT.md) для подробных примеров создания и управления ключами.
